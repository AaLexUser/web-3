<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <!-- Default welcome file -->
    <welcome-file-list>
        <welcome-file>index.xhtml</welcome-file>
    </welcome-file-list>

    <!-- Faces Servlet -->
    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>javax.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- Map Faces Servlet to *.xhtml URLs -->
    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
    </servlet-mapping>

<!--    resource reference tells the application server (in this case, WildFly)-->
<!--    that you want to inject a JDBC data source into your application,-->
<!--    and that the data source should be looked up using the JNDI name "jdbc/lab3"-->
    <resource-ref>
        <description>JDBC data source for the "lab3" database, which manages a table named "entities"
            containing beans named "Entry" with fields "double x, double y, double r, String hitResult"
            and primary key named id.</description>
        <res-ref-name>jdbc/lab3</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>
</web-app>